{
  "name": "pide",
  "displayName": "PiDE",
  "description": "Pi Development Environment: a dedicated VS Code interface for the Pi coding agent.",
  "version": "0.3.2",
  "publisher": "pidaddylabs",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/spacemanspiff-713/pi-de.git"
  },
  "homepage": "https://github.com/spacemanspiff-713/pi-de#readme",
  "bugs": {
    "url": "https://github.com/spacemanspiff-713/pi-de/issues"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "keywords": [
    "pide",
    "pi",
    "development-environment",
    "agent",
    "chat",
    "coding",
    "mcp"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Pi can execute commands and load project-local resources, so a trusted workspace is required."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "Pi requires a filesystem-backed workspace and local process execution."
    }
  },
  "main": "./dist/src/extension.js",
  "activationEvents": [],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "pide",
          "title": "PiDE",
          "icon": "assets/PiDE.jpg"
        }
      ]
    },
    "views": {
      "pide": [
        {
          "type": "webview",
          "id": "pide.chatView",
          "name": "PiDE Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "pide.openChat",
        "category": "PiDE",
        "title": "Open Chat"
      },
      {
        "command": "pide.newSession",
        "category": "PiDE",
        "title": "New Session",
        "icon": "$(add)"
      },
      {
        "command": "pide.openSession",
        "category": "PiDE",
        "title": "Open Session",
        "icon": "$(history)"
      },
      {
        "command": "pide.reviewChanges",
        "category": "PiDE",
        "title": "Review Changes",
        "icon": "$(diff-multiple)"
      },
      {
        "command": "pide.abort",
        "category": "PiDE",
        "title": "Stop Agent",
        "icon": "$(debug-stop)"
      },
      {
        "command": "pide.restart",
        "category": "PiDE",
        "title": "Restart Agent",
        "icon": "$(refresh)"
      },
      {
        "command": "pide.askSelection",
        "category": "PiDE",
        "title": "Ask Pi About Selection"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "pide.newSession",
          "group": "navigation@1",
          "when": "view == pide.chatView"
        },
        {
          "command": "pide.openSession",
          "group": "navigation@2",
          "when": "view == pide.chatView"
        },
        {
          "command": "pide.reviewChanges",
          "group": "navigation@3",
          "when": "view == pide.chatView"
        },
        {
          "command": "pide.restart",
          "group": "navigation@4",
          "when": "view == pide.chatView"
        }
      ],
      "editor/context": [
        {
          "command": "pide.askSelection",
          "group": "navigation@50",
          "when": "editorHasSelection"
        }
      ]
    },
    "configuration": {
      "title": "PiDE",
      "properties": {
        "pide.executablePath": {
          "type": "string",
          "default": "pi",
          "description": "Path to the Pi executable used for RPC mode."
        },
        "pide.extraArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional arguments passed to Pi before RPC mode starts."
        },
        "pide.approveTrustedWorkspace": {
          "type": "boolean",
          "default": true,
          "description": "Pass --approve to Pi after VS Code Workspace Trust has been granted, enabling trusted project-local Pi resources for this run."
        },
        "pide.showThinking": {
          "type": "boolean",
          "default": true,
          "description": "Show streamed reasoning in a collapsible section."
        },
        "pide.gitCheckpoints": {
          "type": "boolean",
          "default": true,
          "description": "Create a non-destructive Git checkpoint of the current working tree before each new Pi task, enabling per-file diff and revert."
        }
      }
    }
  },
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc -p tsconfig.json && npm run typecheck:webview && npm run build:webview",
    "typecheck:webview": "tsc -p tsconfig.webview.json",
    "build:webview": "esbuild webview/main.ts --bundle --platform=browser --format=iife --target=chrome120 --outfile=media/main.js",
    "watch": "tsc -w -p tsconfig.json",
    "test": "vitest run",
    "test:vscode": "node dist/test/vscode/runTest.js",
    "check": "npm run compile && npm test",
    "check:all": "npm run check && npm run test:vscode",
    "package": "npm run check && vsce package --no-dependencies --allow-missing-repository"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^22.15.0",
    "@types/vscode": "^1.100.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.6.0",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "dompurify": "^3.4.13",
    "highlight.js": "^11.11.2",
    "markdown-it": "^15.0.0"
  },
  "icon": "assets/PiDE.jpg"
}
